{{- range .Values.namespaces }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .name }}
  labels:
    {{- toYaml .labels | nindent 4 }}
    managed-by: helm
    chart: {{ $.Chart.Name }}-{{ $.Chart.Version }}
{{- end }}
